import express from 'express';
import { config, type ImportSource } from './config.js';
import { initSchema, query } from './db.js';
import { importFmcsa, refreshScores } from './importer.js';
import { exportToArkon, exportToSheets, getTopLeads } from './export/webhooks.js';
import { publicScoringRules } from './leads/scoringRules.js';
import { checkSocrataDataset } from './fmcsa/socrata.js';
import { enrichFloridaCarriers } from './enrichment/florida.js';
import { enrichTexasCarriers } from './enrichment/texas.js';
import { getEnrichmentSources, ingestStateRegistryRecords } from './enrichment/service.js';
import type { StateRegistryRecordInput } from './enrichment/registryTypes.js';

const app = express();
app.use(express.json({ limit: '5mb' }));
const importSources: ImportSource[] = ['carrier-daily', 'carrier-all-history', 'company-census'];

function requireAdmin(req: express.Request, res: express.Response, next: express.NextFunction) {
  if (!config.adminApiKey) return next();
  const provided = req.header('x-admin-api-key') || req.query.adminApiKey;
  if (provided !== config.adminApiKey) return res.status(401).json({ ok: false, error: 'Unauthorized' });
  return next();
}
function boolQuery(value: unknown): boolean { return ['true', '1', 'yes', 'y'].includes(String(value ?? '').trim().toLowerCase()); }
function stringArray(value: unknown): string[] { if (Array.isArray(value)) return value.map((item) => String(item).trim()).filter(Boolean); if (typeof value === 'string') return value.split(',').map((item) => item.trim()).filter(Boolean); return []; }
function slug(value: unknown): string { return String(value ?? '').trim().toLowerCase().replace(/&/g, 'and').replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '') || `item-${Date.now()}`; }
function text(value: unknown): string | null { const v = String(value ?? '').trim(); return v || null; }
function color(value: unknown, fallback: string): string { const v = String(value ?? '').trim(); return /^#[0-9a-fA-F]{6}$/.test(v) ? v : fallback; }

function registryInputsFromBody(body: unknown): StateRegistryRecordInput[] {
  if (!body || typeof body !== 'object') return [];
  const payload = body as Record<string, unknown>;
  const stateCode = String(payload.stateCode ?? payload.state ?? '').trim().toUpperCase();
  const sourceName = String(payload.sourceName ?? payload.source ?? 'STATE_REGISTRY').trim();
  const recordsValue = Array.isArray(payload.records) ? payload.records : payload.record ? [payload.record] : [];
  return recordsValue.filter((record): record is Record<string, unknown> => Boolean(record && typeof record === 'object' && !Array.isArray(record))).map((raw) => ({ stateCode, sourceName, searchName: payload.searchName ? String(payload.searchName) : null, carrierId: payload.carrierId ? Number(payload.carrierId) : null, usdotNumber: payload.usdotNumber ? String(payload.usdotNumber) : null, legalName: payload.legalName ? String(payload.legalName) : null, raw }));
}
async function tableExists(tableName: string): Promise<boolean> { const result = await query('select to_regclass($1) as table_name', [`public.${tableName}`]); return Boolean(result.rows[0]?.table_name); }
async function safeCount(tableName: string, whereClause = 'true'): Promise<number> { if (!/^[_a-zA-Z][_a-zA-Z0-9]*$/.test(tableName)) return 0; if (!(await tableExists(tableName))) return 0; const result = await query(`select count(*)::int as count from ${tableName} where ${whereClause}`); return Number(result.rows[0]?.count ?? 0); }

async function ensureAdminTables() {
  await query(`create table if not exists insurance_agencies (id bigserial primary key, agency_key text not null unique, agency_name text not null, legal_name text, display_name text, brand_name text, service_area text, lines_of_business text[] not null default '{}', status text not null default 'ACTIVE', notes text, created_at timestamptz not null default now(), updated_at timestamptz not null default now())`);
  await query(`alter table insurance_agencies add column if not exists website_url text`);
  await query(`alter table insurance_agencies add column if not exists logo_url text`);
  await query(`alter table insurance_agencies add column if not exists primary_color text`);
  await query(`alter table insurance_agencies add column if not exists accent_color text`);
  await query(`create table if not exists insurance_agents (id bigserial primary key, agent_key text not null unique, agency_key text, agent_name text not null, email text, phone text, role text not null default 'AGENT', status text not null default 'ACTIVE', lines_of_business text[] not null default '{}', notes text, created_at timestamptz not null default now(), updated_at timestamptz not null default now())`);
  await query(`insert into insurance_agencies (agency_key, agency_name, legal_name, display_name, brand_name, service_area, lines_of_business, status, notes, website_url, primary_color, accent_color) values ('invicta-capital-group', 'Invicta Capital Group', 'Invicta Capital Group', 'Invicta Capital Group', 'Invicta', 'Nationwide', array['Property & Casualty','Life','Health']::text[], 'ACTIVE', 'Default agency for Invicta commercial insurance lead engine.', 'https://www.invictaprotection.com', '#2563eb', '#22c55e') on conflict (agency_key) do update set agency_name = excluded.agency_name, updated_at = now()`);
}
async function listAgencies() { await ensureAdminTables(); const result = await query(`select a.*, count(ag.id)::int as agent_count from insurance_agencies a left join insurance_agents ag on ag.agency_key = a.agency_key and ag.status = 'ACTIVE' group by a.id order by a.created_at desc`); return result.rows; }
async function listAgents() { await ensureAdminTables(); const result = await query(`select ag.*, a.agency_name from insurance_agents ag left join insurance_agencies a on a.agency_key = ag.agency_key order by ag.created_at desc`); return result.rows; }
async function recentImports() { if (!(await tableExists('import_runs'))) return []; const result = await query(`select source, fetched_count, inserted_count, updated_count, started_at from import_runs order by started_at desc limit 6`); return result.rows; }
async function recentRegistryPulls() { if (!(await tableExists('state_registry_matches'))) return []; const result = await query(`select source_name, state_code, search_name, matched_name, entity_status, created_at from state_registry_matches order by created_at desc limit 6`); return result.rows; }

function adminPageHtml(): string {
  return `<!doctype html><html><head><meta charset="utf-8"/><meta name="viewport" content="width=device-width,initial-scale=1"/><title>Invicta Admin</title><style>body{margin:0;background:#0b1220;color:#e2e8f0;font-family:Arial,sans-serif}.top{border-bottom:1px solid #1e293b;padding:14px 24px;display:flex;justify-content:space-between}.links{display:flex;gap:16px;color:#94a3b8;font-size:13px;flex-wrap:wrap}.wrap{max-width:1240px;margin:0 auto;padding:30px 22px}.card{background:#111a2b;border:1px solid #1e293b;border-radius:12px;padding:18px}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(210px,1fr));gap:14px;margin:18px 0}.two{display:grid;grid-template-columns:1fr 1fr;gap:18px;margin-top:18px}.num{font-size:34px;font-weight:800}.muted{color:#64748b;font-size:13px}.label{color:#cbd5e1;font-size:13px;font-weight:700;margin-top:12px}button{border:0;border-radius:8px;padding:9px 13px;background:#2563eb;color:white;font-weight:700;cursor:pointer}button.gray{background:#1e293b;color:#94a3b8;border:1px solid #283548}button.red{background:#7f1d1d;color:#fecaca}input,select{background:#16202f;border:1px solid #283548;border-radius:8px;padding:9px 11px;color:#e2e8f0}input{width:100%}.form3{display:grid;grid-template-columns:1fr 1fr auto;gap:10px}.form4{display:grid;grid-template-columns:1fr 1fr 120px 120px;gap:10px;margin-top:10px}.form2{display:grid;grid-template-columns:1fr auto;gap:10px;margin-top:10px}table{width:100%;border-collapse:collapse;font-size:13px;margin-top:12px}td,th{border-bottom:1px solid #16202f;padding:10px;text-align:left;vertical-align:middle}th{color:#64748b;text-transform:uppercase;font-size:11px}.hidden{display:none}.ok{color:#4ade80}.bad{color:#f87171}.sw{display:inline-block;width:18px;height:18px;border-radius:99px;border:1px solid #475569;margin-right:4px;vertical-align:middle}.logoPrev{max-width:40px;max-height:28px;border-radius:5px;background:#fff}pre{background:#07111f;border:1px solid #1e293b;border-radius:12px;padding:12px;white-space:pre-wrap;max-height:340px;overflow:auto}@media(max-width:900px){.two,.form3,.form4{grid-template-columns:1fr}.top{display:block}}</style></head><body><div class="top"><b>Invicta Admin</b><div class="links"><span>Overview</span><span>Agencies</span><span>Agents</span><span>API Pulls</span><span>Integrations</span><span id="sessionPill">Signed out</span></div></div><main class="wrap"><section id="loginCard" class="card" style="max-width:520px;margin:60px auto"><h1>Admin Login</h1><p class="muted">Enter the admin key.</p><input id="adminKey" type="password" placeholder="Admin key"/><div style="display:flex;gap:10px;margin-top:12px"><button id="loginBtn" type="button">Open admin</button><button class="gray" id="clearBtn" type="button">Clear</button></div><p id="loginStatus" class="muted"></p></section><section id="dashboard" class="hidden"><div style="display:flex;justify-content:space-between;gap:16px"><div><h1>Overview</h1><p class="muted">Manage agencies, agents, agency branding, API pulls, integrations, and admin operations.</p></div><button class="red" id="logoutBtn" type="button">Logout</button></div><div class="grid"><div class="card"><div class="num" id="agencyCount">-</div><div class="label">Active agencies</div></div><div class="card"><div class="num" id="agentCount">-</div><div class="label">Active agents</div></div><div class="card"><div class="num" id="apiPullCount">-</div><div class="label">API / data pulls</div></div><div class="card"><div class="num" id="readyCount">-</div><div class="label">Ready leads</div></div></div><div class="two"><section class="card"><h2>Agencies & Branding</h2><p class="muted">Add an agency and set its branding like ARKON agency setup.</p><div class="form3"><input id="agencyNameInput" placeholder="Agency name"/><input id="agencyServiceInput" value="Nationwide"/><button id="addAgencyBtn" type="button">+ New agency</button></div><div class="form4"><input id="agencyBrandInput" placeholder="Brand name"/><input id="agencyWebsiteInput" placeholder="Website URL"/><input id="agencyPrimaryInput" placeholder="#2563eb" value="#2563eb"/><input id="agencyAccentInput" placeholder="#22c55e" value="#22c55e"/></div><div class="form2"><input id="agencyLogoInput" placeholder="Logo URL"/><button class="gray" type="button" id="previewBrandBtn">Preview</button></div><table><thead><tr><th>Logo</th><th>Agency</th><th>Brand</th><th>Colors</th><th>Agents</th><th>Status</th></tr></thead><tbody id="agencyRows"></tbody></table></section><section class="card"><h2>Agents</h2><p class="muted">Add agents under an agency.</p><div class="form3"><input id="agentNameInput" placeholder="Agent name"/><input id="agentEmailInput" placeholder="Email"/><select id="agentAgencySelect"></select></div><div class="form2"><input id="agentPhoneInput" placeholder="Phone"/><button id="addAgentBtn" type="button">+ New agent</button></div><table><thead><tr><th>Agent</th><th>Email</th><th>Agency</th><th>Status</th></tr></thead><tbody id="agentRows"></tbody></table></section></div><div class="two"><section class="card"><h2>Recent API / data pulls</h2><button class="gray" id="refreshBtn" type="button">Refresh</button><table><thead><tr><th>Source</th><th>Details</th><th>Date</th></tr></thead><tbody id="pullRows"></tbody></table></section><section class="card"><h2>Integration status</h2><table><tbody id="integrationRows"></tbody></table></section></div><div class="two"><section class="card"><h2>Administrator operations</h2><div style="display:grid;gap:10px"><button class="gray" id="dbInitBtn" type="button">Initialize DB schema</button><button id="importBtn" type="button">Import 1,000 carriers</button><button id="scoreBtn" type="button">Refresh scores</button><div style="display:grid;grid-template-columns:1fr 90px;gap:8px"><button id="txBtn" type="button">Run Texas verification</button><select id="txLimit"><option>10</option><option>25</option><option>50</option></select></div><div style="display:grid;grid-template-columns:1fr 90px;gap:8px"><button id="flBtn" type="button">Run Florida verification</button><select id="flLimit"><option>10</option><option>25</option><option>50</option></select></div><button class="gray" id="arkonBtn" type="button">Test CRM export: 1 lead</button></div></section><section class="card"><h2>Audit output</h2><pre id="output">Ready.</pre></section></div></section></main><script>const loginCard=document.getElementById('loginCard'),dashboard=document.getElementById('dashboard'),output=document.getElementById('output');function k(){return sessionStorage.getItem('FMCSA_ADMIN_KEY')||''}function h(){return{'content-type':'application/json','x-admin-api-key':k()}}function f(v){return v===null||v===undefined||v===''?'N/A':String(v)}function yn(v){return v?'<span class="ok">Configured</span>':'<span class="bad">Not configured</span>'}function esc(v){return String(v??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]))}function out(v){output.textContent=typeof v==='string'?v:JSON.stringify(v,null,2)}async function api(p,o){const r=await fetch(p,o||{}),t=await r.text();let d;try{d=JSON.parse(t)}catch(e){d={raw:t}}if(!r.ok)throw new Error(d.error||t);return d}async function get(p){return api(p,{headers:h()})}async function post(p,b){return api(p,{method:'POST',headers:h(),body:JSON.stringify(b||{})})}function show(){loginCard.classList.add('hidden');dashboard.classList.remove('hidden');sessionPill.textContent='Signed in'}function hide(){dashboard.classList.add('hidden');loginCard.classList.remove('hidden');sessionPill.textContent='Signed out'}async function load(){const d=await get('/admin/overview'),m=d.metrics;agencyCount.textContent=f(m.agencies.active);agentCount.textContent=f(m.agents.active);apiPullCount.textContent=f(m.apiPulls.total);readyCount.textContent=f(m.leads.salesReady);integrationRows.innerHTML='<tr><td>Admin auth</td><td>'+yn(d.integrations.adminApiKeyConfigured)+'</td></tr><tr><td>CRM webhook</td><td>'+yn(d.integrations.arkonWebhookConfigured)+'</td></tr><tr><td>Sheets webhook</td><td>'+yn(d.integrations.googleSheetsWebhookConfigured)+'</td></tr><tr><td>Texas API key</td><td>'+yn(d.integrations.txComptrollerKeyConfigured)+'</td></tr>';pullRows.innerHTML=(d.recentPulls||[]).length?d.recentPulls.map(x=>'<tr><td>'+esc(f(x.source))+'</td><td>'+esc(f(x.details))+'</td><td>'+esc(f(x.date))+'</td></tr>').join(''):'<tr><td colspan="3">No pull history yet.</td></tr>';agencyRows.innerHTML=(d.agencies||[]).map(a=>'<tr><td>'+(a.logo_url?'<img class="logoPrev" src="'+esc(a.logo_url)+'"/>':'-')+'</td><td>'+esc(f(a.agency_name))+'<br><span class="muted">'+esc(f(a.website_url))+'</span></td><td>'+esc(f(a.brand_name))+'</td><td><span class="sw" style="background:'+esc(a.primary_color||'#2563eb')+'"></span><span class="sw" style="background:'+esc(a.accent_color||'#22c55e')+'"></span></td><td>'+esc(f(a.agent_count))+'</td><td>'+esc(f(a.status))+'</td></tr>').join('');agentRows.innerHTML=(d.agents||[]).length?d.agents.map(a=>'<tr><td>'+esc(f(a.agent_name))+'</td><td>'+esc(f(a.email))+'</td><td>'+esc(f(a.agency_name||a.agency_key))+'</td><td>'+esc(f(a.status))+'</td></tr>').join(''):'<tr><td colspan="4">No agents yet.</td></tr>';agentAgencySelect.innerHTML=(d.agencies||[]).map(a=>'<option value="'+esc(f(a.agency_key))+'">'+esc(f(a.agency_name))+'</option>').join('')}async function login(){sessionStorage.setItem('FMCSA_ADMIN_KEY',adminKey.value.trim());try{await get('/admin/overview');show();await load()}catch(e){sessionStorage.removeItem('FMCSA_ADMIN_KEY');loginStatus.textContent=e.message}}async function run(label,fn){out(label+'...');try{const d=await fn();out(d);await load()}catch(e){out({ok:false,error:e.message})}}loginBtn.onclick=login;clearBtn.onclick=()=>{adminKey.value='';sessionStorage.removeItem('FMCSA_ADMIN_KEY')};logoutBtn.onclick=()=>{sessionStorage.removeItem('FMCSA_ADMIN_KEY');hide()};refreshBtn.onclick=()=>run('Refreshing admin overview',load);previewBrandBtn.onclick=()=>out({agency:agencyNameInput.value,brand:agencyBrandInput.value,website:agencyWebsiteInput.value,logo:agencyLogoInput.value,primaryColor:agencyPrimaryInput.value,accentColor:agencyAccentInput.value});dbInitBtn.onclick=()=>run('Initializing schema',()=>post('/admin/db/init',{}));importBtn.onclick=()=>run('Importing carriers',()=>post('/admin/import',{source:'company-census',limit:1000}));scoreBtn.onclick=()=>run('Refreshing scores',()=>post('/admin/score/refresh',{}));txBtn.onclick=()=>run('Running Texas verification',()=>post('/admin/enrich/texas',{limit:Number(txLimit.value)}));flBtn.onclick=()=>run('Running Florida verification',()=>post('/admin/enrich/fl',{limit:Number(flLimit.value)}));arkonBtn.onclick=()=>run('Testing CRM export',()=>post('/admin/export/arkon',{limit:1,minGrade:'B'}));addAgencyBtn.onclick=()=>run('Creating agency',()=>post('/admin/agencies',{agencyName:agencyNameInput.value,serviceArea:agencyServiceInput.value,brandName:agencyBrandInput.value,websiteUrl:agencyWebsiteInput.value,logoUrl:agencyLogoInput.value,primaryColor:agencyPrimaryInput.value,accentColor:agencyAccentInput.value}));addAgentBtn.onclick=()=>run('Creating agent',()=>post('/admin/agents',{agentName:agentNameInput.value,email:agentEmailInput.value,phone:agentPhoneInput.value,agencyKey:agentAgencySelect.value}));if(k()){show();load().catch(e=>out({ok:false,error:e.message}))}</script></body></html>`;
}

app.get('/admin', (_req, res) => res.type('html').send(adminPageHtml()));
app.get('/admin/overview', requireAdmin, async (_req, res, next) => { try { await ensureAdminTables(); const [agencyCount, agentCount, carrierCount, leadCount, hotLeadCount, readyLeadCount, importRunCount, registryPullCount, imports, registry, agencies, agents] = await Promise.all([safeCount('insurance_agencies', "status = 'ACTIVE'"), safeCount('insurance_agents', "status = 'ACTIVE'"), safeCount('fmcsa_carriers'), safeCount('insurance_leads'), safeCount('insurance_leads', "lead_grade in ('A+', 'A')"), safeCount('insurance_leads', 'sales_ready = true'), safeCount('import_runs'), safeCount('state_registry_matches'), recentImports(), recentRegistryPulls(), listAgencies(), listAgents()]); const recentPulls = [...imports.map((row) => ({ source: row.source ?? 'FMCSA', details: `fetched ${row.fetched_count ?? 0}, inserted ${row.inserted_count ?? 0}, updated ${row.updated_count ?? 0}`, date: row.started_at })), ...registry.map((row) => ({ source: `${row.source_name ?? 'STATE'} ${row.state_code ?? ''}`.trim(), details: `${row.search_name ?? row.matched_name ?? 'registry pull'} - ${row.entity_status ?? 'unknown'}`, date: row.created_at }))].sort((a, b) => String(b.date ?? '').localeCompare(String(a.date ?? ''))).slice(0, 8); res.json({ ok: true, admin: { signedInAs: 'Verified administrator', role: 'System Administrator', agencyName: 'Invicta Capital Group', agencyId: config.defaultAgencyId }, integrations: { adminApiKeyConfigured: Boolean(config.adminApiKey), arkonWebhookConfigured: Boolean(config.arkonWebhookUrl), googleSheetsWebhookConfigured: Boolean(config.googleSheetsWebhookUrl), txComptrollerKeyConfigured: Boolean(config.txComptrollerApiKey) }, metrics: { agencies: { active: agencyCount }, agents: { active: agentCount }, apiPulls: { total: importRunCount + registryPullCount, importRuns: importRunCount, registryPulls: registryPullCount }, carriers: carrierCount, leads: { total: leadCount, hot: hotLeadCount, salesReady: readyLeadCount } }, recentPulls, agencies, agents }); } catch (error) { next(error); } });
app.post('/admin/agencies', requireAdmin, async (req, res, next) => { try { await ensureAdminTables(); const agencyName = String(req.body?.agencyName ?? req.body?.name ?? '').trim(); if (!agencyName) return res.status(400).json({ ok: false, error: 'agencyName is required.' }); const agencyKey = slug(req.body?.agencyKey ?? agencyName); const result = await query(`insert into insurance_agencies (agency_key, agency_name, legal_name, display_name, brand_name, service_area, website_url, logo_url, primary_color, accent_color, lines_of_business, status) values ($1,$2,$2,$2,$3,$4,$5,$6,$7,$8,array['Property & Casualty','Life','Health']::text[],'ACTIVE') on conflict (agency_key) do update set agency_name = excluded.agency_name, legal_name = excluded.legal_name, display_name = excluded.display_name, brand_name = excluded.brand_name, service_area = excluded.service_area, website_url = excluded.website_url, logo_url = excluded.logo_url, primary_color = excluded.primary_color, accent_color = excluded.accent_color, status = 'ACTIVE', updated_at = now() returning *`, [agencyKey, agencyName, text(req.body?.brandName) ?? agencyName, text(req.body?.serviceArea) ?? 'Nationwide', text(req.body?.websiteUrl), text(req.body?.logoUrl), color(req.body?.primaryColor, '#2563eb'), color(req.body?.accentColor, '#22c55e')]); res.json({ ok: true, agency: result.rows[0] }); } catch (error) { next(error); } });
app.post('/admin/agents', requireAdmin, async (req, res, next) => { try { await ensureAdminTables(); const agentName = String(req.body?.agentName ?? req.body?.name ?? '').trim(); if (!agentName) return res.status(400).json({ ok: false, error: 'agentName is required.' }); const email = String(req.body?.email ?? '').trim(); const agentKey = slug(req.body?.agentKey ?? (email || agentName)); const result = await query(`insert into insurance_agents (agent_key, agency_key, agent_name, email, phone, role, status, lines_of_business) values ($1,$2,$3,$4,$5,$6,'ACTIVE',array['Property & Casualty','Life','Health']::text[]) on conflict (agent_key) do update set agency_key = excluded.agency_key, agent_name = excluded.agent_name, email = excluded.email, phone = excluded.phone, role = excluded.role, status = 'ACTIVE', updated_at = now() returning *`, [agentKey, String(req.body?.agencyKey ?? config.defaultAgencyId), agentName, email || null, String(req.body?.phone ?? '').trim() || null, String(req.body?.role ?? 'AGENT')]); res.json({ ok: true, agent: result.rows[0] }); } catch (error) { next(error); } });
app.get('/admin/config/status', requireAdmin, (_req, res) => res.json({ ok: true, admin: { signedInAs: 'Verified administrator', role: 'System Administrator', agencyName: 'Invicta Capital Group', agencyId: config.defaultAgencyId }, integrations: { adminApiKeyConfigured: Boolean(config.adminApiKey), arkonWebhookConfigured: Boolean(config.arkonWebhookUrl), googleSheetsWebhookConfigured: Boolean(config.googleSheetsWebhookUrl), txComptrollerKeyConfigured: Boolean(config.txComptrollerApiKey) } }));
app.get('/health', (_req, res) => res.json({ ok: true, service: 'fmcsa-insurance-leads' }));
app.get('/scoring/rules', (_req, res) => res.json({ ok: true, scoring: publicScoringRules() }));
app.get('/admin/datasets/check', requireAdmin, async (req, res, next) => { try { const requestedSource = req.query.source ? String(req.query.source) as ImportSource : undefined; const sources = requestedSource ? [requestedSource] : importSources; const invalid = sources.filter((source) => !importSources.includes(source)); if (invalid.length) return res.status(400).json({ ok: false, error: `Invalid source: ${invalid.join(', ')}` }); const results = await Promise.all(sources.map((source) => checkSocrataDataset(source))); res.json({ ok: true, results }); } catch (error) { next(error); } });
app.post('/admin/db/init', requireAdmin, async (_req, res, next) => { try { await initSchema(); await ensureAdminTables(); res.json({ ok: true }); } catch (error) { next(error); } });
app.post('/admin/import', requireAdmin, async (req, res, next) => { try { const source = (req.body?.source ?? config.defaultImportSource) as ImportSource; const limit = Number.parseInt(String(req.body?.limit ?? config.importLimit), 10); const result = await importFmcsa(source, Number.isFinite(limit) ? limit : config.importLimit); res.json({ ok: true, result }); } catch (error) { next(error); } });
app.post('/admin/score/refresh', requireAdmin, async (_req, res, next) => { try { const result = await refreshScores(); res.json({ ok: true, result }); } catch (error) { next(error); } });
app.get('/admin/enrichment/sources', requireAdmin, async (_req, res, next) => { try { const sources = await getEnrichmentSources(); res.json({ ok: true, sources }); } catch (error) { next(error); } });
app.post('/admin/enrich/state-records', requireAdmin, async (req, res, next) => { try { const records = registryInputsFromBody(req.body); if (!records.length) return res.status(400).json({ ok: false, error: 'Provide stateCode, sourceName, and record or records[].' }); const invalid = records.filter((record) => !record.stateCode || !record.sourceName); if (invalid.length) return res.status(400).json({ ok: false, error: 'stateCode and sourceName are required.' }); const result = await ingestStateRegistryRecords(records); res.json(result); } catch (error) { next(error); } });
app.post('/admin/enrich/texas', requireAdmin, async (req, res, next) => { try { const limit = Number.parseInt(String(req.body?.limit ?? config.texasEnrichmentLimit), 10); const result = await enrichTexasCarriers({ limit: Number.isFinite(limit) ? limit : config.texasEnrichmentLimit, usdotNumbers: stringArray(req.body?.usdotNumbers ?? req.body?.usdotNumber), records: Array.isArray(req.body?.records) ? req.body.records : undefined }); res.json(result); } catch (error) { next(error); } });
app.post('/admin/enrich/fl', requireAdmin, async (req, res, next) => { try { const limit = Number.parseInt(String(req.body?.limit ?? 25), 10); const result = await enrichFloridaCarriers({ limit: Number.isFinite(limit) ? limit : 25, usdotNumbers: stringArray(req.body?.usdotNumbers ?? req.body?.usdotNumber), records: Array.isArray(req.body?.records) ? req.body.records : undefined }); res.json(result); } catch (error) { next(error); } });
app.get('/leads', async (req, res, next) => { try { const limit = Math.min(500, Number.parseInt(String(req.query.limit ?? 100), 10) || 100); const minGrade = String(req.query.minGrade ?? 'B'); const qualityGate = boolQuery(req.query.qualityGate); const leads = await getTopLeads(limit, minGrade, qualityGate); res.json({ ok: true, count: leads.length, qualityGate, leads }); } catch (error) { next(error); } });
app.post('/admin/export/arkon', requireAdmin, async (req, res, next) => { try { const result = await exportToArkon(Number(req.body?.limit ?? 100), String(req.body?.minGrade ?? 'B')); res.json({ ok: true, result }); } catch (error) { next(error); } });
app.post('/admin/export/sheets', requireAdmin, async (req, res, next) => { try { const result = await exportToSheets(Number(req.body?.limit ?? 100), String(req.body?.minGrade ?? 'B')); res.json({ ok: true, result }); } catch (error) { next(error); } });
app.get('/stats', async (_req, res, next) => { try { const result = await query(`select (select count(*)::int from fmcsa_carriers) as carriers, (select count(*)::int from insurance_leads) as leads, (select count(*)::int from insurance_leads where lead_grade in ('A+', 'A')) as hot_leads, (select count(*)::int from insurance_leads where sales_ready = true) as sales_ready_leads, (select max(started_at) from import_runs) as last_import_at`); res.json({ ok: true, stats: result.rows[0] }); } catch (error) { next(error); } });
app.use((error: unknown, _req: express.Request, res: express.Response, _next: express.NextFunction) => { const message = error instanceof Error ? error.message : String(error); console.error(error); res.status(500).json({ ok: false, error: message }); });
app.listen(config.port, () => { console.log(`FMCSA insurance lead service listening on ${config.port}`); });
